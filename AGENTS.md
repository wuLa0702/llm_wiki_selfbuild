# LLM Wiki Agent 行为规则

## 核心规则
1. **禁止修改 raw/ 目录**下的任何文件，仅可读取
2. **只能写入 wiki/ 目录**，禁止写入其他任何位置
3. 优先**更新现有 Wiki 页面**，禁止随意新建重复页面
4. 所有关键结论必须**标注引用来源**（格式：`../raw/sources/xxx.md`）
5. 不确定的内容标记为「⚠️ 待补充」，不强行下结论
6. 页面关联需合理，禁止强制链接无关页面
7. 更新 Wiki 后必须同步更新 `index.md` 和 `log.md`
8. 禁止删除任何 Wiki 页面，废弃页面标记 `[已废弃]` 即可
9. 实体页放在 `entities/`，概念页放在 `concepts/`，摘要放在 `sources/`

## 操作流程

### Ingest 流程
1. 用 read 工具读取 raw/ 下的文件
2. 提取实体、概念、核心观点
3. 在对应目录下创建/更新 Wiki 页面
4. 使用 `[[双向链接]]` 关联相关页面
5. 更新 index.md 和 log.md

### Query 流程
1. 搜索 Wiki 定位相关页面
2. 组装上下文
3. 带引用回答
4. 好问答归档到 queries/

### Lint 流程
1. 检查孤儿页（无入链）
2. 检查断链（指向不存在的页面）
3. 检查过时内容（>30 天未更新）
4. 输出健康报告，不自动修改
