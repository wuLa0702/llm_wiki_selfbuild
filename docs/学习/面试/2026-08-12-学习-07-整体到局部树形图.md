# 07 · 整体到局部树形图


> 📋 **规范**：遵循 `docs/规则/文档规范.md`
> 📌 **更新时间**：2026-08-12
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1 | 2026-08-12 | 初版（迁移自旧 docs，仅补规范头）|

> 用途：面试时"从整体到局部"的讲述骨架——面试官问任何一个点，你都能指出它在树上的位置，然后钻进去。
> 五棵树，从大到小：项目 → 代码结构 → Agent 四层 → 图节点 → 状态字段。每层一句话。

---

## 树 1：项目整体（30 秒讲完这棵）

```mermaid
graph TD
    P["LLM Wiki 知识编译系统<br/>（不是 RAG：一次编译多次查询）"]
    P --> IN["摄入子系统<br/>两步 CoT + SHA256 缓存<br/>→ wiki/ 结构化页面"]
    P --> AG["Agent 问答子系统<br/>LangGraph 9 节点 ReAct<br/>三层自纠错 + 双轨记忆"]
    P --> SE["检索子系统<br/>BM25 + 向量 + RRF<br/>混合搜索"]
    P --> KG["知识图谱子系统<br/>wikilink → 4-Signal<br/>→ Louvain 社区 + 洞察"]
    P --> FE["前端子系统<br/>React 9 页面 SPA"]
    P --> SYS["系统支撑<br/>安全模型 / MCP / 打包"]
    IN --> AG
    IN --> KG
    SE --> AG
    KG --> SE
    AG --> FE
```

**记忆口诀**：摄入产出知识 → Agent 消费知识 → 图谱让知识互联 → 前端让人使用。

---

## 树 2：代码结构（src/ 六块）

```mermaid
graph TD
    S["src/（Python 后端）"]
    S --> A["agent/ — LangGraph Agent<br/>感知→规划→记忆→行动 四层"]
    S --> API["api/ — FastAPI 路由<br/>11 个模块，/v1/ 前缀"]
    S --> C["core/ — 核心逻辑<br/>compiler / graph / embedding / cache / auth"]
    S --> L["llm/ — LLM 适配<br/>LLMAdapter，provider 注册表"]
    S --> D["db/ — 数据层<br/>schema 16 表 + WikiRepository"]
    S --> T["tools/ — 可复用 IO<br/>safe_path / Read / Search / Write / Lint"]
    A -->|调用| T
    C -->|调用| L
    C -->|调用| T
    C -->|调用| D
    API -->|编排| C
    API -->|调用| A
```

**记忆口诀**：API 是门面，core 是业务，agent 是大脑，tools 是手，llm 是嘴，db 是仓库。

---

## 树 3：Agent 四层（`src/agent/`）

```mermaid
graph TD
    A["src/agent/ — Agent 模块"]
    A --> PER["perception/ 感知层<br/>handler 消息序列化<br/>intent 意图分类"]
    A --> PLA["planning/ 规划层<br/>graph 9 节点状态机<br/>prompt 提示词 + 结构化模型"]
    A --> MEM["memory/ 记忆层<br/>attention Sink 锚定<br/>store SQLite 持久化<br/>summarizer 摘要压缩"]
    A --> ACT["action/ 行动层<br/>registry 工具注册表<br/>tools 3 只读工具<br/>stream SSE 事件<br/>response 结构化回答"]
    A --> SES["session.py 编排层<br/>chat_stream_session 主入口<br/>协调四层完成一轮对话"]
    PLA --> MEM
    PLA --> ACT
    SES --> PER
    SES --> PLA
    SES --> MEM
```

**记忆口诀**：消息进来 → 感知层认（意图）→ 规划层想（选节点）→ 记忆层调（回忆）→ 行动层做（调工具）→ session 串全场。

---

## 树 4：9 节点图（徒手画这棵）

```mermaid
graph TD
    E["__start__"]
    E --> N1["① intent_classifier<br/>规则意图分类"]
    N1 --> N2["② agent (call_model)<br/>注入记忆上下文 + 计数"]
    N2 --> N3["③ extract_wm<br/>提取事实/目标"]
    N3 --> N4["④ wm_eviction<br/>槽位清理"]
    N4 --> C1{"有 tool_calls?"}
    C1 -->|否| N8["⑧ summarizer → END"]
    C1 -->|是| N5["⑤ validate_tool<br/>L1: 熔断 + 去重"]
    N5 --> C2{"校验结果?"}
    C2 -->|超限| N8
    C2 -->|重复| N2
    C2 -->|通过| N6["⑥ approve<br/>风险分级 HITL"]
    N6 --> C3{"审批?"}
    C3 -->|拒绝| N2
    C3 -->|批准| N7["⑦ tools<br/>ToolNode 执行"]
    N7 --> N9["⑨ verify_result<br/>L2: 质检"]
    N9 --> C4{"质量?"}
    C4 -->|差| N10["⑩ reflect_node<br/>L3: LLM 反思"]
    N10 --> N2
    C4 -->|通过| N2
```

**三句话讲完**：① 意图分流（闲聊不走工具）→ ② 前置校验（熔断/去重）→ ③ 后置质检（差则反思重试）。**背这三个"分流点"就记住了整张图。**

---

## 树 5：AgentState 七字段（钻到最细）

```mermaid
graph TD
    ST["AgentState (TypedDict)"]
    ST --> M["messages<br/>Annotated[list, add_messages]<br/>增量追加"]
    ST --> S["attention_sinks<br/>用户驱动锚定<br/>置信度衰减"]
    ST --> W["working_memory<br/>Annotated[dict, 自定义 reducer]<br/>5 槽位: 身份/目标/事实/工具缓存/实体"]
    ST --> SC["step_count<br/>熔断计数"]
    ST --> EA["executed_actions<br/>防重复哈希表<br/>key=工具名:规范化参数"]
    ST --> SEL["self_correction<br/>L1/L2 结果传递"]
    ST --> CI["current_intent<br/>意图分类结果"]
```

**记忆口诀**：2 个记忆（sink + WM）+ 1 个消息流 + 3 个控制（步数/去重/修正标记）+ 1 个意图。

---

## 追问：这五棵树怎么组合讲？

> 面试官从任何一点切入，用"树定位法"回答：
>
> - 问"你的 Agent 怎么工作的" → 树 3 → 树 4（四层 → 节点图）
> - 问"状态怎么管理" → 树 5（七字段逐个讲）
> - 问"整个项目流程" → 树 1 → 树 2（子系统 → 代码结构）
> - 问"某个具体功能" → 先定位在树 2 的哪块，再钻到树 3/4/5 的对应叶子
>
> 练习：随机抽一个问题，5 秒内说出"它在树的哪个位置"——这就是面试的"心中有图"。

---

*维护提示：树形图随代码演进更新；新增节点/字段时保持"每层一句话 + 记忆口诀"格式，别让树长成森林。*
