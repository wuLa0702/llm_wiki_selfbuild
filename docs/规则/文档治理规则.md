# 文档治理规则 v1（docs 唯一治理规则）

> 📋 **规范**：本文件是 docs 治理规则唯一载体（**权威在 `.claude/rules/05-docs-placement.md`**，双位置镜像，冲突以 .claude/rules/ 为准）
> 📌 **更新时间**：2026-08-12
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1 | 2026-08-12 | 初版：自 multi_agent `docs/规则/文档治理规则.md` 移植，按 wiki 实际文件适配——目录全中文（decisions→决策/、features→规则|归档/、legacy→归档/、testing→评审/）、新增 `评审/` 目录承接测试计划/报告、命名规则（动态顶层无前缀 / 过程带日期+vN）、**双向链接规则**、索引维护 |

> **目录**：
> - §1 目录结构（全中文）
> - §2 类型 × 目录表
> - §3 命名规则（动态 vs 过程）
> - §4 双向链接规则 ★
> - §5 归档流程
> - §6 索引维护
> - §7 权威 vs 镜像（双位置）

> **关联**（双向）：`.claude/rules/05-docs-placement.md`（权威）｜ `docs/索引.md`（唯一入口）｜ `规则/文档规范.md`（格式规则唯一载体）

---

## 1. 目录结构（全中文） 🔴 v1

```
docs/
├── 索引.md                    # 唯一入口（动态，无前缀）
├── 功能更新日志.md              # 变更记录（动态，无前缀）
├── 架构/                       # 现状真相源（动态，无前缀）
├── 规则/                       # 规则统一管理（动态，无前缀）
│   ├── 文档规范.md               # 格式规则唯一载体
│   ├── SOP.md                    # 六步工作流
│   ├── 文档治理规则.md            # 本文件（.claude/rules/05 镜像）
│   └── 功能模板.md               # 功能立项模板
├── 评审/                       # 测试计划 / 测试报告
├── 决策/                       # 决策记录（决策-）
├── 学习/                       # 学习笔记 / 能力梳理
├── 踩坑/                       # 踩坑与操作（How-to）
├── 归档/                       # 退出决策链（历史留存）
├── 参考/                       # 外部参考资料
├── 临时/                       # 暂存区（仅活跃清单）
└── 图片和附件/                  # 图片 / OCR 原始附件
```

> 目录名一律**中文**；`decisions/`、`features/`、`legacy/`、`testing/` 已迁移清除（2026-08-12）。

---

## 2. 类型 × 目录表 🔴 v1

| 类型前缀 | 目录 | 说明 |
|----------|------|------|
| （无前缀）| `架构/`、`规则/`、docs 根 | 动态维护文档（命名见 §3）|
| 测试计划- | `评审/` | 测试计划（过程文档）|
| 测试报告- | `评审/` | 实测结果报告 |
| 决策- | `决策/` | 决策记录（做/不做），不转正 |
| 学习- | `学习/` | 学习笔记 / 能力梳理 |
| 踩坑- | `踩坑/` | 踩坑与操作（`YYYY-MM-DD-主题.md`）|
| 架构- | `架构/` | 现状真相源 |
| 规则- | `规则/` | 规则文档（无前缀，见 §3）|

---

## 3. 命名规则（动态 vs 过程） 🔴 v1

| 文档性质 | 命名 | 例 |
|----------|------|-----|
| **动态维护**（顶层 / 规则 / 索引 / 日志）| **`主题.md`**（无日期、无 -vN）| `索引.md`、`文档规范.md` |
| **过程性**（测试计划/报告 / 学习 / 决策记录 / 踩坑）| `YYYY-MM-DD-类型-主题.md` | `2026-08-01-测试计划-测试计划.md`、`2026-07-27-学习-agent架构图.md` |
| **踩坑** | `YYYY-MM-DD-主题.md`（沿用）| `2026-07-20-踩坑-wiki-lint分析.md` |

> 动态文档的版本靠文档头 📝 版本记录（只追加不删除）；文件名不带版本，避免"动态文件却叫 v1"误导。

---

## 4. 双向链接规则 ★ 🔴 v1

**三层要求**：

| 层 | 规则 | 检查 |
|----|------|------|
| **关联成对** | A 引用 B ⇒ B 反向引用 A（关联段必须成对出现）| 新增/改关联时，同步补对方文档的反向链接 |
| **索引 ↔ 文档** | 索引表里每个文档一行；**每个文档头部必须有"回索引"链接**（关联段含 `docs/索引.md`）| 新增文档即加索引行 + 回链 |
| **权威 ↔ 镜像** | `.claude/rules/` ↔ `规则/` 双位置文档互相标注"权威在 X / 镜像" | 改权威必须同步镜像（或注明冲突以权威为准）|

**落地**：每个文档头部规范头下方固定放 `> **关联**（双向）：…` 段——既有链接列全，反向链接一并列出。

---

## 5. 归档流程 🔴 v1

| 场景 | 动作 |
|------|------|
| **执行完使命 / 被覆盖** | `git mv` 入 `归档/`（保历史，不删）+ 索引状态 📦 |
| **改造过程文档** | 规则部分抽入 `规则/`，执行记录归档 `归档/` |
| **顶层动态文档** | 永不归档（活着），只更新内容 + 版本记录 |

---

## 6. 索引维护 🔴 v1

- `索引.md` 是 docs 唯一入口：新增 / 改名 / 归档文档时**同步更新一行**
- 索引本身是动态文档：**无前缀尾缀**（§3）
- 索引 ↔ 文档双向链接（§4）
- 索引每节（架构/规则/评审/决策/学习/踩坑/归档/参考/临时）列对应目录全部文件

---

## 7. 权威 vs 镜像（双位置） 🔴 v1

| 位置 | 角色 | 说明 |
|------|------|------|
| `.claude/rules/`（现有 8 个）| **机器加载权威** | Claude Code 自动加载，规则生效处 |
| `规则/`（docs 侧）| 人类可读镜像 + 统一入口 | 与 .claude/rules/ 对应文档互为镜像 |

**冲突处理**：以 `.claude/rules/` 为准；改规则必须两处同步或注明差异。

---

> 📌 **维护**：规则本身变更 → 版本记录追加 + `.claude/rules/05-docs-placement.md` 同步（镜像）。
