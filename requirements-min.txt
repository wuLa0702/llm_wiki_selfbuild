# 最小运行依赖清单（云服务器 / 远程部署用）
#
# 原则：只保留运行时必需的轻量依赖，排除内存大户与测试依赖。
#   ❌ 排除 chromadb / sentence-transformers —— 语义搜索（向量 Embedding）本地模型
#      会加载 torch 全家桶（~2GB+ 内存），云服务器资源有限，默认不装。
#      embedding 功能默认关闭（EMBEDDING_ENABLED=false / 设置页开关默认 off），
#      需要语义搜索的机器请改用完整 requirements.txt。
#   ❌ 排除 langchain-community —— LLM 适配器仅用 langchain_core + langchain_openai。
#   ❌ 排除 pytest / pytest-mock —— 仅测试用，部署环境不需要。
#
# 依赖必须与 requirements.txt 保持同源版本约束；新增依赖时两边同步更新。

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
sqlite-utils>=3.0
markdown>=3.0.0
pypdf>=5.0.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
python-dotenv>=1.0.0
networkx>=3.0
python-louvain>=0.16
python-pptx>=1.0.0
openpyxl>=3.1.0
pyyaml>=6.0
rank-bm25>=0.2.0
